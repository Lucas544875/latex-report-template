% 依存パッケージ comment
%引用:https://qiita.com/rityo_math/items/efd44bc8f9229e014237
\DeclareMathOperator{\Ker}{Ker}
\DeclareMathOperator{\res}{Res}
\let\Im\relax%衝突回避
\DeclareMathOperator{\Im}{Im}
\DeclareMathOperator{\llog}{\mathrm{Log}}
\newcommand{\define}{\overset{def}{\Leftrightarrow}}%要修正
\newcommand{\C}{\mathbb{C}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\F}{\mathcal{F}}
\newcommand{\relmiddle}[1]{\mathrel{}\middle#1\mathrel{}}
\begin{comment}
  集合の縦割り記号\middle の代わりに \relmiddle を付ける
  \( \mathbb{P} = \left\{ x \in \mathbb{N} \relmiddle| x : prime \right\} \)
\end{comment}
%\renewcommand{\thesubsection}{(\alph{subsection})}%項番号を小文字アルファベットで出力
\allowdisplaybreaks[4]
\begin{comment}
  \lstset{
    style=customlst,
    language=Python
  }みたいにして使える
\end{comment}
\lstdefinestyle{customlst}{%listingsの設定
  %背景色
	backgroundcolor={\color[HTML]{eaeaea}},
	%標準の書体 7pt
	basicstyle = \ttfamily\scriptsize,
	%空白で自動改行
  breakatwhitespace=false,
	%枠外に行った時の自動改行
	breaklines = true,
	%キャプションの場所("tb"ならば上下両方に記載)
	captionpos = t,
	%コメントの書体
	commentstyle = {\color[HTML]{505050}},
	%除外するキーワード
  %deletekeywords={},
	% Latexエスケープ
  escapeinside={\%*}{*)},
	%行番号の開始番号
  %firstnumber=1000,
	%枠 "t"は上に線を記載, "T"は上に二重線を記載
  %他オプション：leftline，topline，bottomline，lines，single，shadowbox
	frame = tBrL,
	%空白を保持 (possibly needs columns=flexible)
  %keepspaces=true,
	%キーワード(int, ifなど)の書体
	keywordstyle = [1]{\color[HTML]{4876d6}},%青
	keywordstyle = [2]{\color[HTML]{aa0982}},%緑
	keywordstyle = [3]{\color[HTML]{08916a}},%紫
	keywordstyle = [4]{\color[HTML]{A31515}},%茶
	%プログラム言語(複数の言語に対応，C,C++も可)lstsetの方で指定してね
 	%language = Python,
	%追加するキーワード
	%morekeywords={},
	%行番号の配置 (none, left, right)
  numbers=left,
	%行番号からコードの間隔
  %numbersep=10pt,
	%行番号の書体 5pt
	numberstyle = {\ttfamily\tiny\color{gray}},
	% if not set, the frame-color may be changed on line-breaks within not-black text (e.g. comments (green here))
  rulecolor=\color{black},
	% show spaces everywhere adding particular underscores; it overrides 'showstringspaces'
  showspaces=false,
	%文字列の中だけスペースを強調表示
  showstringspaces=false,
	%タブの強調表示
  showtabs=false,
	%行番号の間隔
	stepnumber = 1,
	%""で囲まれたなどの"文字リテラル"の書体
	stringstyle = {\color[HTML]{bc5454}},
	%タブの大きさ
	tabsize = 2,
 	%自動改行後のインデント量(デフォルトでは20[pt])	
 	breakindent = 10pt,
 	%関数名等の色の設定
 	classoffset = 0,
 	%frameまでの間隔(行番号とプログラムの間)
 	%framesep = 5pt,
	%右マージン
 	%xrightmargin=0zw,
 	%左マージン
	%xleftmargin=3zw,
	columns=fixed,
	basewidth=0.5em,
}
\lstdefinelanguage{S-program}{
  morekeywords=[1]{if,goto,END},%キーワード
  morekeywords=[2]{++,--},%リテラル
  morekeywords=[3]{},%クラス
  morekeywords=[4]{},%その他
  morecomment=[l]{//},%行コメント
  morecomment=[s]{/*}{*/},%複数行コメント
  morestring=[b]{'},%文字列リテラル、/でエスケープ
}
\lstdefinelanguage{GLSL}{
  morekeywords=[1]{precision,mediump,uniform},%キーワード
  morekeywords=[2]{min},%組み込み関数
  morekeywords=[3]{float,vec2,vec4,void},%クラス
  morekeywords=[4]{},%その他
  morecomment=[l]{//},%行コメント
  morecomment=[s]{/*}{*/},%複数行コメント
  morestring=[b]{'},%文字列リテラル、/でエスケープ
}

\definecolor{burgundy}{rgb}{0.5, 0.0, 0.13}
\tcbset{
	mytheo/.style={
		fonttitle=\gtfamily\fontseries{bx}\selectfont\upshape,
		enhanced,colframe=burgundy,colback=burgundy!2!white,colbacktitle=burgundy,
		boxrule=0pt,borderline south={2pt}{-2pt}{burgundy},
		left*=1zw,right*=1zw,
		theorem style=standard,
		breakable,sharp corners,
		before skip=8pt,
		after skip=10pt,
		before upper={\setlength{\parindent}{1zw}},
		before lower={\setlength{\parindent}{1zw}}
	}
}
%Theorem
\newtcbtheorem[number within=section]{theorem}{定理}%
{mytheo}{th}
\newcommand{\thref}[1]{{\fontseries{bx}\selectfont 定理 \ref{th:#1}}}
%Proposition
\newtcbtheorem[use counter from=theorem]{prop}{命題}%
{mytheo}{pr}
\newcommand{\prref}[1]{{\fontseries{bx}\selectfont 命題 \ref{pr:#1}}}
%Corollary
\newtcbtheorem[use counter from=theorem]{cor}{系}%
{mytheo}{co}
\newcommand{\coref}[1]{{\fontseries{bx}\selectfont 系 \ref{co:#1}}}
%Definition
\newtcbtheorem[use counter from=theorem]{definition}{定義}%
{mytheo,
colframe=blue!50!black,colback=blue!50!black!2!white,colbacktitle=blue!50!black,borderline south={2pt}{-2pt}{blue!50!black},}{de}
\newcommand{\deref}[1]{{\fontseries{bx}\selectfont 定義 \ref{de:#1}}}
%Lemma
\newtcbtheorem[use counter from=theorem]{lemma}{補題}%
{mytheo,
colframe=green!50!black,colback=green!50!black!2!white,colbacktitle=green!50!black,borderline south={2pt}{-2pt}{green!50!black},}{le}
\newcommand{\leref}[1]{{\fontseries{bx}\selectfont 補題 \ref{le:#1}}}
%Example
\definecolor{charcoal}{rgb}{0.21, 0.27, 0.31}
\newtcbtheorem[use counter from=theorem]{example}{例}%
{mytheo,
colframe=charcoal,colback=charcoal!2!white,colbacktitle=charcoal,borderline south={2pt}{-2pt}{charcoal},}{ex}
\newcommand{\exref}[1]{{\fontseries{bx}\selectfont 例 \ref{ex:#1}}}
\newcommand{\figref}[1]{{\fontseries{bx}\selectfont 図 \ref{fig:#1}}}
\newcommand{\tableref}[1]{{\fontseries{bx}\selectfont 表 \ref{table:#1}}}

\newtheoremstyle{report}%% スタイル名
	{}%                    % 上部スペース
	{}%                    % 下部スペース
	{\normalfont}%         % 本文フォント
	{}%                    % 1行目のインデント量
	{\fontseries{bx}\selectfont}%見出しフォント
	{.}%                   % 見出し後の句読点
	{5pt}%                 % 見出し後のスペース
  {\thmname{#1}\thmnumber{#2}\thmnote{\hspace{2pt}(#3)}}
%字下げするようにしたい

\theoremstyle{report}
\newtheorem{question}{問題}
\newtheorem{question+}{問題}[section]
\newtheorem*{question*}{問題}
%\newtheorem{theorem}{定理}
%\newtheorem*{theorem*}{定理}
\newtheorem*{answer}{解答}

\renewcommand\labelenumi{(\arabic{enumi})}
\renewcommand\labelenumii{(\alph{enumii})}
\renewcommand\labelenumiii{(\roman{enumiii})}